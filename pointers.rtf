1. closures- function nested inside a func has access to varible of outside function
2. temporal dead zones- if u try to access variable defined with let or const before initialization you will get a reference error. not same for var. temporal dead zones, allow you to create block-scoped variables.